# --- Local (API directo en tu PC / red) ---
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
# NEXT_PUBLIC_WS_URL=http://localhost:3000
# NEXT_PUBLIC_WS_ENABLED=true

# --- Vercel (HTTPS) + EC2 (HTTP): proxy same-origin ---
# GATEWAY_ORIGIN se usa solo en el build (rewrites). No va al browser.
GATEWAY_ORIGIN=http://3.144.151.209:3000
NEXT_PUBLIC_API_URL=/gateway
NEXT_PUBLIC_WS_URL=
NEXT_PUBLIC_WS_ENABLED=true

# OneSignal — misma App ID que ms-notificaciones
NEXT_PUBLIC_ONESIGNAL_APP_ID=
